Standardize naming and structure across the repository.

Read all source files; fix: snake_case (files/functions/variables), PascalCase (classes), UPPER_SNAKE_CASE (constants), absolute imports, Google-style docstrings on public APIs, loguru instead of print().
