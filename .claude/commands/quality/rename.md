Safely rename across the codebase: $ARGUMENTS (format: OldName -> NewName)

Search all source files, tests, docs for every occurrence of the old name.
Rename: definition, all call sites, imports, __init__.py exports, docstrings, comments.
