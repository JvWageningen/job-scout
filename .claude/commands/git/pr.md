Draft a pull request for: $ARGUMENTS

Run `git log --oneline main..HEAD` and `git diff main...HEAD --stat` to summarize the branch.
Generate a PR title and markdown body with: Summary, Changes (from commits), Version impact (patch/minor/major based on commit flags), and Testing notes.
