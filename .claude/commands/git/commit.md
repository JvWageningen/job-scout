Create a commit for: $ARGUMENTS

Run `git diff --cached --stat` to see staged changes. If nothing staged, suggest files to stage based on `git status --short`.
Write a conventional commit message (feat/fix/chore/docs/test/refactor). If changes touch source code, ask whether to add `[patch]`, `[minor]`, or `[major]` flag — patch=bugfix, minor=new feature, major=breaking change. Commit with the chosen message.
