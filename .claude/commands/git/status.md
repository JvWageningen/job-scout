Summarize the current git and project state.

Run: `cat VERSION`, `git branch --show-current`, `git status --short`, `git log --oneline -5`, `git stash list`.
Report: current version, branch, staged/unstaged/untracked counts, recent commits, stashes. Flag if tree is dirty or branch is behind remote.
