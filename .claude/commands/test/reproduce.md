Generate a minimal reproducing test for: $ARGUMENTS

Read relevant source files. Write the smallest pytest test that: sets up minimum state, triggers the bug, asserts incorrect behavior (test MUST FAIL with bug present).
Run to confirm it fails; add to appropriate test file.
