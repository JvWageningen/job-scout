Generate tests for: $ARGUMENTS

Read the source file; create/update matching test file in tests/.
Test every public function: happy path, edge cases, error handling; use fixtures and parametrize.
Mock external deps (network, file I/O, APIs). Use specific exception types in pytest.raises(). Name tests test_<function>_<scenario>.
