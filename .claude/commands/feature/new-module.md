Create `src/{{PACKAGE_NAME}}/$ARGUMENTS.py` with module docstring, absolute imports, type-hinted stubs, Google-style docstrings on public members.
Create `tests/test_$ARGUMENTS.py` with at least one test per public function.
