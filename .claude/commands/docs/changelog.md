Generate or update CHANGELOG.md: $ARGUMENTS (version number, e.g. 1.2.0)

Run `git log --oneline --no-merges` since last tag. Group into: Added, Changed, Fixed, Removed.
Write or prepend a version section in CHANGELOG.md using Keep a Changelog format.
